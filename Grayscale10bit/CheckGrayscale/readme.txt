README for CheckGrayscale
- To Run, type 
CheckGrayscale

This will print out a listing of all attached displays and open a window on each of them with a teapot drawn
When moving or resizing the window between the displays the event handler determines if the window is spanning a grayscale compatible display.

